@model IEnumerable<ShoppingCart.Areas.Shop.Models.ItemDTO>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "ItemList";
    Layout = "_ShopLayout";
}

<h2>ItemList</h2>

<p>
    <a asp-action="CreateItem" asp-controller="Item" asp-route-id="@TempData["id"] ">Create New</a>
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@
        @foreach (var item in Model)
        {
            <div class="col-lg-4 col-md-4 col-sm-4">
                <div class="thumbnail">
                    <img src="~/itemImages/@item.ItemCategoryId/@item.ImageUrl1" alt="errrroor" style="width:350px; height:300px;" />
                    <div class="caption">
                        <h3 class="pull-right">@item.UnitPrice.ToString("c")</h3>
                        <h3>
                            <a>@item.ItemName</a>
                        </h3>
                        <p>@item.Description</p>
                        <p>@item.SignUpId</p>
                        <p>@item.ItemName</p>
                        <p>@item.ImageUrl1</p>
                        <div>
                            <a asp-controller="Item" asp-action="Edit" asp-route-id="@item.SignUpId" asp-route-edit="@item.ItemId">Edit</a>
                            <a asp-controller="Item" asp-action="ItemDetail" asp-route-id="@item.ItemId">Details</a>
                            <a asp-controller="Item" asp-action="Delete" asp-route-id="@item.SignUpId" asp-route-delete="@item.ItemId">Delete</a>
                        </div>
                    </div>
                </div>

            </div>

            @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ItemName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ItemCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ImageUrl)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UnitPrice) 
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a asp-action="Edit" asp-controller="Item" asp-route-id="@TempData["id"]" asp-route-edit="@item.ItemId">Edit</a> |
                    
                    <a asp-action="Delete" asp-controller="Item" asp-route-id="@TempData["id"]" asp-route-delete="@item.ItemId">Delete</a>
                </td>
            </tr>*@
        }

    @*</tbody>
</table>*@
